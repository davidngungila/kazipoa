import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0F00E7); // Match HTML exactly
  static const Color background = Color(0xFF000000); // Solid black like HTML
  static const Color surface = Color(0xFF1A1A1A);
  static const Color onSurface = Color(0xFFF8FAFC); // Match HTML on-surface
  static const Color error = Color(0xFFE53E3E);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  
  // Additional HTML-matching colors
  static const Color surfaceContainer = Color(0xFF111111);
  static const Color surfaceContainerHigh = Color(0xFF1E293B);
  static const Color onSurfaceVariant = Color(0xFF94A3B8);
  static const Color primaryContainer = Color(0x1A0F00E7); // rgba(15, 0, 231, 0.1)
}
